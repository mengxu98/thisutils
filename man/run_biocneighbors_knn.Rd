% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BiocNeighborsKNN.R
\name{run_biocneighbors_knn}
\alias{run_biocneighbors_knn}
\title{Find nearest neighbors with BiocNeighbors}
\usage{
run_biocneighbors_knn(
  reference,
  query = NULL,
  k,
  metric = c("euclidean", "cosine"),
  exclude_self = FALSE,
  n_threads = 1L
)
}
\arguments{
\item{reference}{Numeric reference matrix with observations in rows.}

\item{query}{Optional numeric query matrix with the same number of columns.}

\item{k}{Number of nearest neighbors to return.}

\item{metric}{Distance metric: `"euclidean"` or `"cosine"`.}

\item{exclude_self}{Whether to remove self-neighbors when `query` is `NULL`.}

\item{n_threads}{Number of BiocNeighbors threads.}
}
\value{
A list with integer matrix `idx` and numeric matrix `dist`.
}
\description{
Find nearest neighbors within a reference matrix or from a query matrix to a
reference matrix. The wrapper standardizes matrix coercion, distance metric,
and self-neighbor removal across consumers.
}
\examples{
if (requireNamespace("BiocNeighbors", quietly = TRUE)) {
  run_biocneighbors_knn(matrix(rnorm(20), ncol = 2), k = 2)
}
}
