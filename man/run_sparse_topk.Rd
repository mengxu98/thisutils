% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MatrixTopK.R
\name{run_sparse_topk}
\alias{run_sparse_topk}
\title{Sparse matrix top-k selection}
\usage{
run_sparse_topk(
  x,
  k,
  by = c("col", "row"),
  decreasing = TRUE,
  include_implicit_zeros = TRUE
)
}
\arguments{
\item{x}{A \code{dgCMatrix} (or something coercible to one).}

\item{k}{Number of top entries to retain per column or row. Must be a positive
integer.}

\item{by}{Direction of selection: \code{"col"} ranks row entries independently
within each column, while \code{"row"} ranks column entries independently within
each row.}

\item{decreasing}{Whether to sort in decreasing order (largest values
first). Default is \code{TRUE}.}

\item{include_implicit_zeros}{Whether unstored sparse positions participate
as zeros. The default, \code{TRUE}, gives ordinary matrix semantics. Set to
\code{FALSE} to rank only stored entries; for that use case, prefer the more
explicit \code{\link[=run_sparse_topk_stored]{run_sparse_topk_stored()}}.}
}
\value{
A list with two components:
\describe{
\item{idx}{Integer matrix of 1-based indices into the opposite dimension.}
\item{value}{Numeric matrix of corresponding values.}
}
Both matrices have \code{ncol(x)} rows when \code{by = "col"} and \code{nrow(x)} rows
when \code{by = "row"}.
}
\description{
For each column or row of a sparse \code{dgCMatrix}, extract the top \code{k} matrix
elements and their indices. By default, unstored positions participate with
their matrix value of zero. For column-wise selection, this gives the same
semantics as \code{\link[=run_dense_topk_by_column]{run_dense_topk_by_column()}}.
}
\examples{
m <- Matrix::rsparsematrix(10, 20, density = 0.3)
run_sparse_topk(m, k = 3, by = "col")
run_sparse_topk(m, k = 3, by = "row")
}
