% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallelize_fun.R
\name{detect_cores}
\alias{detect_cores}
\title{Detect the number of CPU cores}
\usage{
detect_cores(
  cores = NULL,
  num_session = NULL,
  max_threads = NULL,
  logical = FALSE
)
}
\arguments{
\item{cores}{Requested number of workers. Only used when \code{num_session} is
supplied; invalid values fall back to \code{1}.}

\item{num_session}{Number of tasks that can be scheduled concurrently. When
supplied, the result is the usable worker count: the detected cores minus one
reserved for the parent, capped by \code{cores} and \code{num_session}.}

\item{max_threads}{Optional upper bound for the detected core count, e.g. the
largest worker count a kernel can use. \code{NULL} leaves the detected value as is.}

\item{logical}{Whether to report logical (hyper-threaded) cores, as
\code{\link[parallel:detectCores]{parallel::detectCores()}} does.}
}
\value{
A single integer, at least \code{1}. A failed or unusable probe falls back
to \code{1} instead of propagating \code{NA}.
}
\description{
Detect the number of CPU cores
}
\examples{
detect_cores()
detect_cores(max_threads = 2)
detect_cores(cores = 4, num_session = 8)
}
