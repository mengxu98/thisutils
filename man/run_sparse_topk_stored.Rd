% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MatrixTopK.R
\name{run_sparse_topk_stored}
\alias{run_sparse_topk_stored}
\title{Stored sparse entries top-k selection}
\usage{
run_sparse_topk_stored(x, k, by = c("col", "row"), decreasing = TRUE)
}
\arguments{
\item{x}{A \code{dgCMatrix} (or something coercible to one).}

\item{k}{Number of top entries to retain per column or row. Must be a positive
integer.}

\item{by}{Direction of selection: \code{"col"} ranks row entries independently
within each column, while \code{"row"} ranks column entries independently within
each row.}

\item{decreasing}{Whether to sort in decreasing order (largest values
first). Default is \code{TRUE}.}
}
\value{
A list with \code{idx} and \code{value} matrices as described in
\code{\link[=run_sparse_topk]{run_sparse_topk()}}. Groups with fewer than \code{k} stored entries are padded
with \code{NA} in both matrices.
}
\description{
For each column or row of a sparse \code{dgCMatrix}, rank only explicitly stored
entries. Unstored zeros never become candidates. This is useful for sparse
adjacency matrices, where stored entries represent graph edges rather than
samples from a full numeric matrix.
}
\examples{
graph <- Matrix::sparseMatrix(
  i = c(1, 3, 2), j = c(1, 1, 2), x = c(1, 3, -2),
  dims = c(3, 2)
)
run_sparse_topk_stored(graph, k = 2, by = "col")
}
