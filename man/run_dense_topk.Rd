% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MatrixTopK.R
\name{run_dense_topk}
\alias{run_dense_topk}
\title{Dense matrix top-k selection}
\usage{
run_dense_topk(x, k, by = c("col", "row"), decreasing = TRUE)
}
\arguments{
\item{x}{A numeric matrix (or something coercible to one).}

\item{k}{Number of entries to retain per column or row. Must be a positive
integer.}

\item{by}{Direction of selection: \code{"col"} ranks row entries independently
within each column, while \code{"row"} ranks column entries independently within
each row.}

\item{decreasing}{Whether to sort in decreasing order (largest values
first). Default is \code{TRUE}.}
}
\value{
A list with two components:
\describe{
\item{idx}{Integer matrix of 1-based indices into the opposite dimension.}
\item{value}{Numeric matrix of corresponding values.}
}
Both matrices have \code{ncol(x)} rows when \code{by = "col"} and \code{nrow(x)} rows
when \code{by = "row"}.
}
\description{
For each column or row of a dense numeric matrix, extract the top \code{k}
elements and their indices. The default returns the largest values; set
\code{decreasing = FALSE} to retain the smallest values, as in nearest-neighbor
selection from a distance matrix.
}
\examples{
m <- matrix(rnorm(100), nrow = 10)
run_dense_topk(m, k = 3, by = "col")
run_dense_topk(m, k = 3, by = "row")
run_dense_topk(m, k = 3, by = "col", decreasing = FALSE)
}
