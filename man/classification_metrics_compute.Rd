% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ClassificationMetrics.R
\name{classification_metrics_compute}
\alias{classification_metrics_compute}
\title{Compute classification metrics}
\usage{
classification_metrics_compute(predicted, truth, rare_threshold = 0.05)
}
\arguments{
\item{predicted}{Character vector of predicted class labels.}

\item{truth}{Character vector of true class labels (same length as
\code{predicted}).}

\item{rare_threshold}{Proportion threshold for rare-class recall.
Classes with support proportion \eqn{<=} this value contribute to the
\code{rare_recall} metric. Default is \code{0.05}.}
}
\value{
A list with the following components:
\describe{
\item{accuracy}{Overall accuracy (scalar).}
\item{macro_f1}{Macro-averaged F1 score (scalar).}
\item{purity}{Cluster purity (scalar).}
\item{nmi}{Normalized Mutual Information (scalar).}
\item{ari}{Adjusted Rand Index (scalar).}
\item{rare_recall}{Mean recall on rare classes (scalar, or \code{NA} if no rare classes).}
\item{class_table}{A data frame of per-class precision, recall, F1,
and support.}
}
}
\description{
Compute multi-class classification metrics from predicted and true labels,
including accuracy, macro-F1, purity, NMI, ARI, and rare-class recall.
}
\examples{
predicted <- c("A", "A", "B", "B", "C")
truth <- c("A", "B", "B", "B", "C")
classification_metrics_compute(predicted, truth)
}
